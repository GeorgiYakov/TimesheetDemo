<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiApp1"
             x:Class="MauiApp1.AppShell"
             Title="AppShell"
             BackgroundColor="White"
             FlyoutBehavior="Locked" 
             FlyoutWidth="200"
       >
    <FlyoutItem Title="Daily Tasks">
        <ShellContent ContentTemplate="{DataTemplate local:MonthlyTasksPage}"
                      Route="Tasks" />
    </FlyoutItem>
    <FlyoutItem Title="Forecast">
        <ShellContent ContentTemplate="{DataTemplate local:ForecastPage}"
                      Route="ForecastPage" />
    </FlyoutItem>
    <FlyoutItem Title="Projects">
        <ShellContent ContentTemplate="{DataTemplate local:ProjectsPage}"
                      Route="Projects" />
    </FlyoutItem>
    <FlyoutItem Title="Workdays">
        <ShellContent ContentTemplate="{DataTemplate local:WorkdaysPage}"
                      Route="Workdays" />
    </FlyoutItem>
    
</Shell>